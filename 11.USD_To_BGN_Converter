using System;

class USD_To_BGN_Convertor
{
    static void Main()
    {
        Console.Write("USD = ");
        double usd = double.Parse(Console.ReadLine());
        double bgn = usd * 1.79549;
        Console.WriteLine("{0} BGN", Math.Round(bgn, 2));

    }
}
